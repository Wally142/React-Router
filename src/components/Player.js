import React from 'react';

const Player = (props) => {
    
    return (
        <div>
            {/* <h1>{player.name} (#{player.number})</h1>
            <h2>{player.position}</h2> */}
        </div>
    )
}

export default Player;